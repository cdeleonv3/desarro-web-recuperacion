## Notas
- Utilizando Django “puro” usando únicamente las vistas basadas en funciones (FBV). 
- Utilizando el sistema de usuarios propio de Django (django.contrib.auth)

## Credenciales
usuario `Inge` pass `IvanDeLeon`
usuario `Revisor` pass `IngeIvan`